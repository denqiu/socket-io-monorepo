/**
 * 1. Run pnpm test:client
 * 2. Test warnings by enabling/disabling event helper event types.
 * 3. Test backend and frontend in separate terminals vs all in 1 terminal.
 * 4. Update logging logic to send to mysql db.
 */